---
title: "Symmetric Matrix From Half-Vectorization"
author: "Ivan Jacob Agaloos Pesigan"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Symmetric Matrix From Half-Vectorization}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(linearAlgebra)
```

```{r}
(x <- matrix(c("a", "b", "b", "c"), ncol = 2))
vech_i <- c("a", "b", "c")
vechs_i <- "b"
diags_i <- c("a", "c")
```

## Symmetric Matrix From Half-Vectorization

### From Half-Vectorization

```{r}
sym_of_vech(vech_i)
```

### Symmetric Matrix From Strict Half-Vectorization

```{r}
sym_of_vechs(vechs_i, diags = diags_i)
```
